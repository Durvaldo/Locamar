const ContainerMainPage = ({className}) => {

    return (
        <main className={`${className} flex flex-col items-center`}>
            Container
        </main>
    );
}

export default ContainerMainPage